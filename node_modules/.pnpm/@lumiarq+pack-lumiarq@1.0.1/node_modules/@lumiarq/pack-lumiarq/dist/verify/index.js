import { checkBoundary, checkBuild, checkExports, checkNaming, checkPlacement, checkTypeScript, } from '@lumiarq/verifier';
/**
 * Verification hooks enforced for LumiARQ step execution.
 *
 * The hook chain is intentionally split by execution phase:
 * - `before-write` prevents unsafe or malformed writes from occurring.
 * - `after-write` validates the generated artifact and the resulting IR state.
 */
export const lumiarqVerificationHooks = [
    {
        name: 'lumiarq.build.preflight',
        when: 'before-write',
        run: checkBuild,
    },
    {
        name: 'lumiarq.placement.prewrite',
        when: 'before-write',
        run: checkPlacement,
    },
    {
        name: 'lumiarq.naming.prewrite',
        when: 'before-write',
        run: checkNaming,
    },
    {
        name: 'lumiarq.exports.postwrite',
        when: 'after-write',
        run: checkExports,
    },
    {
        name: 'lumiarq.typescript.postwrite',
        when: 'after-write',
        run: checkTypeScript,
    },
    {
        name: 'lumiarq.boundary.postwrite',
        when: 'after-write',
        run: checkBoundary,
    },
];
//# sourceMappingURL=index.js.map