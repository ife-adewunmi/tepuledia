import { BaseRepository } from '@lumiarq/framework/database';
import type { I{{ClassName}}Repository } from '../contracts/repositories/{{fileName}}.repository';

export class {{ClassName}}Repository extends BaseRepository implements I{{ClassName}}Repository {
  async findById(id: string): Promise<unknown | null> {
    return null;
  }

  async create(data: unknown): Promise<unknown> {
    return { id: '{{fileName}}_id', ...data };
  }

  async update(id: string, data: unknown): Promise<unknown> {
    return { id, ...data };
  }

  async delete(id: string): Promise<void> {
    // Remove resource by id
  }
}
