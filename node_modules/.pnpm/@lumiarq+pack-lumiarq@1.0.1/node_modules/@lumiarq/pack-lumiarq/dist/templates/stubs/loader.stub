import { defineLoader } from '@lumiarq/framework';
import { z } from 'zod';

/**
 * {{ClassName}} loader — fetches and prepares data for rendering.
 * Receives input from the runtime pipeline and returns app context.
 */
export const {{ClassName}}Loader = defineLoader({
  id: '{{ClassName}}',
  schema: z.object({
    // Define input schema here
  }),

  async fetch({ input }): Promise<Record<string, unknown>> {
    void input; // Remove when implementing
    return {};
  },
});
