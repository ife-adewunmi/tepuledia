import { defineEvent } from '@lumiarq/framework';
import { z } from 'zod';

export const {{ClassName}} = defineEvent({
  name: '{{Name}}',
  schema: z.object({
    // Define event payload schema here
  }),
});
