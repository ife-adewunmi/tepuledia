import { defineHandler } from '@lumiarq/framework';

/**
 * {{ClassName}} handler — HTTP request handler that bridges the router to application logic.
 * Converts request to action input and returns response to the client.
 */
export const {{ClassName}} = defineHandler(
  async (request: Request): Promise<Response> => {
    void request; // Remove when implementing
    return new Response(JSON.stringify({ message: 'ok' }), {
      status: 200,
      headers: { 'Content-Type': 'application/json' },
    });
  },
);
