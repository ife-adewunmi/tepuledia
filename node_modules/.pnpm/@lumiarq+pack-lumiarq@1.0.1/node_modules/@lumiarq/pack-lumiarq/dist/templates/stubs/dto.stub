/**
 * {{ClassName}} DTO — Data Transfer Object for serialization across boundaries.
 * Defines the shape of {{ClassName}} when transported between layers.
 */
export interface {{ClassName}} {
  id: string;
  createdAt: Date;
  updatedAt: Date;
}
