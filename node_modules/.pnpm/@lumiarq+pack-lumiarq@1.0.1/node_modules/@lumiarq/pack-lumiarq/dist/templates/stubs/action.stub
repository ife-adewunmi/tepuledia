import { defineAction } from '@lumiarq/framework';

/**
 * {{ClassName}} action — orchestrates business logic flow.
 * Actions compose tasks, call repositories, and emit domain events.
 */
export const {{ClassName}} = defineAction(
  async (input: unknown): Promise<unknown> => {
    void input; // Remove when implementing
    return input;
  },
);
