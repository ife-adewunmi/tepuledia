import { z } from 'zod';

/**
 * {{ClassName}} validator — validates and parses input data.
 * Re-export a base validator or define custom validation rules.
 */
export const {{ClassName}}Validator = z.object({
  // Define validation schema here
});

export type {{ClassName}}Data = z.infer<typeof {{ClassName}}Validator>;
