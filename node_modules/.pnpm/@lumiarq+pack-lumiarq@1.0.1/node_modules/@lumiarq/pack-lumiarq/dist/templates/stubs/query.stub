import { defineQuery } from '@lumiarq/framework';

/**
 * {{ClassName}} query — read-only access to application state.
 * Queries return data without side effects and are typically cached.
 */
export const {{ClassName}} = defineQuery(async (input: unknown): Promise<unknown> => {
  void input; // Remove when implementing
  return input;
});
