import { defineTask } from '@lumiarq/framework';

/**
 * {{ClassName}} task — reusable business logic that performs a discrete operation.
 * Tasks are synchronous units of work composed by actions, loaders, and other tasks.
 */
export const {{ClassName}} = defineTask(async (input: unknown): Promise<unknown> => {
  void input; // Remove when implementing
  return input;
});
