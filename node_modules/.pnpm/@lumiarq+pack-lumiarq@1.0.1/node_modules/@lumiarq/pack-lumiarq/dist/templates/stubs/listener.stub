import { defineListener } from '@lumiarq/framework';

/**
 * {{ClassName}} listener — responds to domain events.
 * Handle side effects and downstream actions here.
 */
export const {{ClassName}} = defineListener({
  async handle(payload: Record<string, unknown>): Promise<void> {
    void payload; // Remove when implementing
    // Process event and trigger side effects
  },
});
