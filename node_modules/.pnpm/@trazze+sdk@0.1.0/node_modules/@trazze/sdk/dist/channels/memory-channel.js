export function createMemoryChannel() {
    let entries = [];
    return {
        handle(entry) {
            entries.push(entry);
        },
        getEntries: () => [...entries],
        clear: () => {
            entries = [];
        },
    };
}
//# sourceMappingURL=memory-channel.js.map