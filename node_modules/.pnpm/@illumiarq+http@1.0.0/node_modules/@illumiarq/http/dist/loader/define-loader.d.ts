import { z, type ZodTypeAny } from 'zod';
import type { BoundLoader, SitemapEntry } from './types.js';
export interface LoaderDefinition<TSchema extends ZodTypeAny, TOutput> {
    id?: string;
    schema: TSchema;
    dependsOn?: BoundLoader[];
    fetch(args: {
        input: z.infer<TSchema>;
        deps: Map<string, unknown>;
    }): Promise<TOutput>;
}
export declare function defineLoader<TSchema extends ZodTypeAny, TOutput>(definition: LoaderDefinition<TSchema, TOutput>): BoundLoader;
export interface SitemapLoaderFn<TDeps = void> {
    (ctx: TDeps extends void ? Record<never, never> : {
        repos: TDeps;
    }): Promise<SitemapEntry[]>;
}
export declare function defineSitemapLoader<TDeps = void>(fn: SitemapLoaderFn<TDeps>): SitemapLoaderFn<TDeps>;
//# sourceMappingURL=define-loader.d.ts.map