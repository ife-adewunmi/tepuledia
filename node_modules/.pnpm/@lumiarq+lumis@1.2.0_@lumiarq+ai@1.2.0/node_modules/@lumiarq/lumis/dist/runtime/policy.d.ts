import type { RuntimeProfile } from '@lumiarq/context';
import type { LumisConfig } from '../config-loader.js';
export type RuntimePolicyMode = 'strict' | 'advisory';
export type ResolvedRuntimePolicy = {
    mode: RuntimePolicyMode;
    requireApprovalForRemote: boolean;
    approvalEnvVar: string;
};
export type RuntimePolicyDecision = {
    mode: RuntimePolicyMode;
    requiresApproval: boolean;
    approved: boolean;
    allowExecution: boolean;
    blockingReason?: string;
};
export declare function resolveRuntimePolicy(config: LumisConfig): ResolvedRuntimePolicy;
export declare function evaluateRuntimePolicy(input: {
    profile: RuntimeProfile;
    config: LumisConfig;
    approveRemote?: boolean;
    processEnv?: NodeJS.ProcessEnv;
}): RuntimePolicyDecision;
//# sourceMappingURL=policy.d.ts.map