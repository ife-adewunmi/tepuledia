import type { LumisAdapter } from '@lumiarq/context';
import type { LumisTraceListener } from '../tracing/types.js';
import type { LumisConfig } from '../config-loader.js';
import { executeOnRuntimeProfile } from './executor.js';
export type RuntimeTaskName = 'typecheck' | 'lint' | 'boundaryCheck' | 'build' | 'test';
export type RuntimeTaskExecution = {
    task: RuntimeTaskName;
    command: string;
    result: Awaited<ReturnType<typeof executeOnRuntimeProfile>>;
    transcriptPath: string;
};
export type RuntimeOrchestrationResult = {
    enabled: boolean;
    profileId?: string;
    profileKind?: string;
    steps: RuntimeTaskExecution[];
    success: boolean;
    errors: string[];
};
export declare function executeRuntimeTasks(input: {
    projectRoot: string;
    adapter: LumisAdapter;
    config: LumisConfig;
    write: (message: string) => void;
    writeError: (message: string) => void;
    enabled: boolean;
    profileId?: string;
    requestedTasks?: RuntimeTaskName[];
    failOnTaskError?: boolean;
    approveRemote?: boolean;
    labelPrefix?: string;
    processEnv?: NodeJS.ProcessEnv;
    onTrace?: LumisTraceListener;
}): Promise<RuntimeOrchestrationResult>;
export declare function runPostMakeRuntimeTasks(input: {
    projectRoot: string;
    adapter: LumisAdapter;
    config: LumisConfig;
    write: (message: string) => void;
    writeError: (message: string) => void;
    onTrace?: LumisTraceListener;
}): Promise<RuntimeOrchestrationResult>;
//# sourceMappingURL=orchestration.d.ts.map