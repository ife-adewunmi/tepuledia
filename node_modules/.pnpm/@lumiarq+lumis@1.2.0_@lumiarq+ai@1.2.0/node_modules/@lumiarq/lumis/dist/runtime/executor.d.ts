import type { RuntimeProfile } from '@lumiarq/context';
export type RuntimeTranscriptEntry = {
    stream: 'stdout' | 'stderr' | 'system';
    text: string;
    timestamp: string;
};
export type RuntimeExecutionRequest = {
    profile: RuntimeProfile;
    command: string;
    args?: string[];
    cwd?: string;
    env?: Record<string, string>;
    timeoutMs?: number;
    processEnv?: NodeJS.ProcessEnv;
};
export type RuntimeExecutionResult = {
    profileId: string;
    profileKind: RuntimeProfile['kind'];
    command: string;
    args: string[];
    success: boolean;
    exitCode: number | null;
    startedAt: string;
    endedAt: string;
    durationMs: number;
    transcript: RuntimeTranscriptEntry[];
    stdout: string;
    stderr: string;
    redactedProfile: RuntimeProfile;
    diagnostics: string[];
};
export declare function executeOnRuntimeProfile(request: RuntimeExecutionRequest): Promise<RuntimeExecutionResult>;
//# sourceMappingURL=executor.d.ts.map