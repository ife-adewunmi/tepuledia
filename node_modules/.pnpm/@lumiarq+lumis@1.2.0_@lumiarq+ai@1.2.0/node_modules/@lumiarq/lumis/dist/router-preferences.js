function readNextjsRouterOverride(config) {
    const configured = config.routing?.nextjs?.router;
    return configured === 'app' || configured === 'pages' ? configured : null;
}
export function applyRouterPreferenceOverrides(ir, adapterName, config) {
    if (adapterName !== 'nextjs') {
        return ir;
    }
    const routerOverride = readNextjsRouterOverride(config);
    if (!routerOverride) {
        return ir;
    }
    return {
        ...ir,
        metadata: {
            ...ir.metadata,
            routerOverride,
            routerPreference: routerOverride,
        },
    };
}
//# sourceMappingURL=router-preferences.js.map