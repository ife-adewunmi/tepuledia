/**
 * Tinker Observability
 *
 * Records agent turns, tool calls, and verification results to Trazze
 * and local structured logs for replay and debugging.
 */
export type TinkerTurnEvent = {
    timestamp: string;
    sessionId: string;
    projectFingerprint: string;
    turnIndex: number;
    eventType: 'user-message' | 'assistant-message' | 'tool-call' | 'tool-result' | 'verification' | 'error';
    data: Record<string, unknown>;
};
export interface TinkerObserver {
    recordTurn(event: TinkerTurnEvent): Promise<void>;
    flush(): Promise<void>;
}
/**
 * Default no-op observer.
 */
export declare const noopObserver: TinkerObserver;
/**
 * Composite observer that chains multiple observers.
 */
export declare class CompositeObserver implements TinkerObserver {
    private observers;
    constructor(observers: TinkerObserver[]);
    recordTurn(event: TinkerTurnEvent): Promise<void>;
    flush(): Promise<void>;
}
/**
 * Trazze push observer — sends events to Trazze via HTTP/gRPC.
 * (Stub: full implementation depends on Trazze client availability.)
 */
export declare class TrazzeObserver implements TinkerObserver {
    private baseUrl;
    private sessionId;
    constructor(baseUrl: string, sessionId: string);
    recordTurn(event: TinkerTurnEvent): Promise<void>;
    flush(): Promise<void>;
}
/**
 * Create an observer based on environment.
 */
export declare function createObserver(sessionId: string): TinkerObserver;
//# sourceMappingURL=tinker-observer.d.ts.map