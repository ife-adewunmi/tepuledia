import type { AIGateway } from '@lumiarq/agent';
export type AgentStepStatus = 'pending' | 'running' | 'done' | 'failed' | 'skipped';
export type AgentStep = {
    id: string;
    description: string;
    status: AgentStepStatus;
    risk?: 'low' | 'medium' | 'high';
    verifyHint?: string;
};
export type AgentPlan = {
    goal: string;
    riskLevel: 'low' | 'medium' | 'high';
    estimatedFiles: string[];
    steps: AgentStep[];
};
export declare const STATUS_ICON: Record<AgentStepStatus, string>;
export declare function normalizeAgentPlan(goal: string, value: unknown): AgentPlan;
export declare function renderAgentPlan(write: (message: string) => void, plan: AgentPlan): void;
export declare function proposeAgentPlan(options: {
    goal: string;
    projectRoot: string;
    framework: string;
    gateway: AIGateway;
    chatModel?: string;
}): Promise<AgentPlan>;
//# sourceMappingURL=tinker-agent-plan.d.ts.map