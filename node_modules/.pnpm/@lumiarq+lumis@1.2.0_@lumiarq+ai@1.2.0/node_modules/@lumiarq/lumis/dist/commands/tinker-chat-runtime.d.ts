import { type AIGateway, type AIMessage } from '@lumiarq/agent';
import { type RAGPipeline } from '@lumiarq/rag';
import type { CommandContext } from './runtime.js';
import { type MemoryStore } from '../lib/tinker-memory.js';
import { type TinkerSessionRuntime } from './tinker-session-runtime.js';
export type TinkerChatSession = TinkerSessionRuntime & {
    packName: string;
    framework: string;
    chatGateway?: AIGateway;
    chatHistory: AIMessage[];
    ragPipeline?: RAGPipeline;
    ragReady: boolean;
    ragInitializing: boolean;
    ragIndexedFiles: number;
    toolCallsEnabled: boolean;
    memoryStore: MemoryStore;
};
type ChatGatewaySession = {
    context: CommandContext;
    chatGateway?: AIGateway;
};
export declare function buildChatSystemMessage(session: Pick<TinkerChatSession, 'context' | 'framework' | 'packName' | 'chatModel' | 'toolCallsEnabled'>): AIMessage;
export declare function resetChatHistory(session: Pick<TinkerChatSession, 'chatHistory'> & Parameters<typeof buildChatSystemMessage>[0]): void;
export declare function looksLikeFilesystemPath(text: string): boolean;
export declare function ensureChatGateway(session: ChatGatewaySession): Promise<AIGateway>;
export declare function initRepoRag(session: TinkerChatSession): Promise<void>;
export declare function retrieveRepoContext(session: TinkerChatSession, text: string): Promise<string | undefined>;
export declare function sendChatMessage(session: TinkerChatSession, text: string): Promise<void>;
export {};
//# sourceMappingURL=tinker-chat-runtime.d.ts.map