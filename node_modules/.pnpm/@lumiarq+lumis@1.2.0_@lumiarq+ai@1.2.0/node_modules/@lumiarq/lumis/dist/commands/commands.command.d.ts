import type { CommandContext } from './runtime.js';
export type NormalizedCommandEntry = {
    source: 'pack' | 'project';
    group: 'framework' | 'project';
    name: string;
    description: string;
    args: Array<{
        name: string;
        required: boolean;
        description: string;
    }>;
};
/**
 * Collect normalized command metadata from both pack and project sources.
 *
 * @param context - Shared command runtime context.
 * @returns Combined normalized command entries sorted by source then name.
 */
export declare function collectNormalizedCommands(context: CommandContext): Promise<NormalizedCommandEntry[]>;
/**
 * List available pack and project commands.
 *
 * @param context - Shared command runtime context.
 * @returns Exit code 0 when listing succeeds.
 */
export declare function executeCommandsListCommand(context: CommandContext): Promise<number>;
/**
 * Run a pack or project command by name.
 *
 * Usage:
 * - `lumis commands run pack <name> [key=value ...]`
 * - `lumis commands run project <name>`
 *
 * @param context - Shared command runtime context.
 * @returns Exit code 0 on success, otherwise 1.
 */
export declare function executeCommandsRunCommand(context: CommandContext): Promise<number>;
//# sourceMappingURL=commands.command.d.ts.map