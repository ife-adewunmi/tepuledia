/**
 * Shared command runtime for the host CLI.
 *
 * This module centralizes config loading, output channels, and pack/adapter
 * resolution so command handlers can focus on behavior instead of plumbing.
 */
import type { LumisAdapter, LumisPackModule } from '@lumiarq/context';
import type { AdapterCommand } from '@lumiarq/context';
import type { LumisTraceListener } from '../tracing/types.js';
import { type LumisConfig } from '../config-loader.js';
export type CommandContext = {
    projectRoot: string;
    commandPath: string[];
    commandArgs: string[];
    argv: string[];
    config: LumisConfig;
    write: (message: string) => void;
    writeError: (message: string) => void;
    resolvePack: () => Promise<LumisPackModule | null>;
    resolveAdapter: () => Promise<LumisAdapter | null>;
    resolvePackCommands: () => Promise<AdapterCommand[]>;
    resolveProjectCommands: () => Array<{
        name: string;
        description: string;
        command: string;
    }>;
    onTrace?: LumisTraceListener;
};
export type CommandHandler = (context: CommandContext) => Promise<number>;
/**
 * Build a reusable command execution context.
 *
 * @param options - Runtime inputs such as argv, command path/args, project root, and output writers.
 * @returns Hydrated command context with lazy pack/adapter resolvers and loaded config.
 */
export declare function createCommandContext(options: {
    argv: string[];
    commandPath?: string[];
    commandArgs?: string[];
    projectRoot: string;
    write?: (message: string) => void;
    writeError?: (message: string) => void;
    onTrace?: LumisTraceListener;
}): Promise<CommandContext>;
//# sourceMappingURL=runtime.d.ts.map