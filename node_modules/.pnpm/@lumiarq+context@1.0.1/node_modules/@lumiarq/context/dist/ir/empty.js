import { CURRENT_IR_VERSION } from '../persistence/ir-version.js';
/**
 * Create an empty IR
 * @param projectRoot - Root directory of the project
 */
export function createEmptyIR(projectRoot) {
    return {
        irVersion: CURRENT_IR_VERSION,
        version: CURRENT_IR_VERSION,
        projectRoot,
        framework: 'unknown',
        builtAt: new Date(),
        modules: [],
        dependencies: [],
        violations: [],
        metadata: {
            builtAt: new Date().toISOString(),
        },
    };
}
//# sourceMappingURL=empty.js.map