/**
 * Build IR using an adapter
 * @param adapter - The framework adapter to use
 * @param projectRoot - Root directory of the project
 */
export async function buildIRWithAdapter(adapter, projectRoot) {
    return adapter.buildIR(projectRoot);
}
/**
 * Update IR using an adapter
 * @param adapter - The framework adapter to use
 * @param ir - Current IR to update
 * @param changedFile - Path to a file that changed
 */
export async function updateIRWithAdapter(adapter, ir, changedFile) {
    return adapter.updateIR(ir, changedFile);
}
//# sourceMappingURL=build.js.map