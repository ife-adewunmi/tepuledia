import type { CoreExecutionStep, CoreLumisIR, DependencyRule, NamingRule, ParsedIntent, PlacementRule, VerificationHook } from './core.types.js';
export type AdapterCommandArg = {
    name: string;
    required: boolean;
    description: string;
};
export type AdapterCommand = {
    name: string;
    description: string;
    args: AdapterCommandArg[];
    run: (args: Record<string, string>, projectRoot: string) => Promise<void>;
};
export type LumisAdapter = {
    name: string;
    version: string;
    language: 'typescript' | 'javascript' | 'python' | 'ruby' | 'php' | string;
    detect: (projectRoot: string) => Promise<{
        detected: boolean;
        confidence: number;
        reason: string;
    }>;
    buildIR: (projectRoot: string) => Promise<CoreLumisIR>;
    updateIR: (ir: CoreLumisIR, changedFile: string) => Promise<CoreLumisIR>;
    placementRules: PlacementRule[];
    dependencyRules: DependencyRule[];
    namingRules: NamingRule[];
    resolveIntent: (intent: ParsedIntent, ir: CoreLumisIR) => Promise<CoreExecutionStep[]>;
    resolveTemplate: (templateName: string) => string;
    verificationHooks: VerificationHook[];
    buildCommands: {
        typecheck?: string;
        lint?: string;
        boundaryCheck?: string;
        build?: string;
        test?: string;
    };
    tinkerContext?: (projectRoot: string) => Promise<Record<string, unknown>>;
    commands?: () => Promise<AdapterCommand[]>;
};
//# sourceMappingURL=adapter.types.d.ts.map