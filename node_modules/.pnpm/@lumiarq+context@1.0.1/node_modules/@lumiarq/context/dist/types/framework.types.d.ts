/**
 * Framework Definition
 *
 * Generic framework contract that adapters implement to describe:
 * - How artifacts map to directories (paths)
 * - Optional aliasing between adapter-defined layer names
 * - Supported artifact kinds
 */
export type FrameworkLayerName = string;
export type FrameworkArtifactKind = string;
/**
 * Framework Definition - describes how artifacts map to directory structure
 */
export interface FrameworkDefinition {
    name: string;
    version: string;
    /** Map adapter-defined artifact kinds to directory paths */
    paths: Partial<Record<FrameworkArtifactKind, string>>;
    /** Optional aliasing between adapter-defined layer names */
    layerAliases?: Partial<Record<FrameworkLayerName, FrameworkLayerName>>;
    /** Optional custom metadata */
    metadata?: Record<string, unknown>;
}
//# sourceMappingURL=framework.types.d.ts.map