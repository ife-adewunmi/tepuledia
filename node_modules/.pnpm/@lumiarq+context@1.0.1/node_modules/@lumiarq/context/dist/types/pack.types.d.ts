import type { LumisAdapter } from './adapter.types.js';
export type LumisPackCapability = 'detection' | 'ir' | 'planning' | 'generation' | 'verification' | 'commands' | 'tinker' | string;
export interface LumisPackManifest {
    name: string;
    version: string;
    lumisCore: string;
    language: 'typescript' | 'javascript' | 'python' | 'ruby' | 'php' | string;
    frameworks: string[];
    detectionPriority: number;
    capabilities: {
        detection: boolean;
        ir: boolean;
        planning: boolean;
        generation: boolean;
        verification: boolean;
        commands: boolean;
        tinker: boolean;
    };
    migrationNotes?: string[];
}
export interface LumisPackModule {
    manifest: LumisPackManifest;
    adapter: LumisAdapter;
}
//# sourceMappingURL=pack.types.d.ts.map