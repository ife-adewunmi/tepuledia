import fs from 'node:fs';
import path from 'node:path';
import { stampCurrentIRVersion } from './ir-version.js';
export function saveIR(projectRoot, ir) {
    const lumisDir = path.join(projectRoot, '.lumis');
    fs.mkdirSync(lumisDir, { recursive: true });
    const stamped = stampCurrentIRVersion(ir);
    fs.writeFileSync(path.join(lumisDir, 'ir.json'), JSON.stringify(stamped, null, 2), 'utf8');
}
//# sourceMappingURL=save.js.map