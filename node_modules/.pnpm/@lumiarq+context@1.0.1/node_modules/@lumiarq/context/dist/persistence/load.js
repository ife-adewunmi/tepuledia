import fs from 'node:fs';
import path from 'node:path';
import { migratePersistedIR } from './ir-version.js';
export function loadIR(projectRoot) {
    const irPath = path.join(projectRoot, '.lumis', 'ir.json');
    if (!fs.existsSync(irPath)) {
        return null;
    }
    const raw = JSON.parse(fs.readFileSync(irPath, 'utf8'));
    return migratePersistedIR(raw);
}
//# sourceMappingURL=load.js.map