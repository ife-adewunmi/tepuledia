export type TemplateStubLoaderOptions = {
    stubsDir: string;
    templateToStubFile: Record<string, string>;
    namespace?: string;
    /**
     * Optional directories checked before pack stubs.
     * Relative directories are resolved against process.cwd() at load time.
     */
    projectOverrideDirs?: string[];
};
export type TemplateStubLoader = {
    listTemplateNames: () => string[];
    loadTemplate: (templateName: string) => string;
};
export declare function resolveDirectoryFromImportMeta(importMetaUrl: string, ...segments: string[]): string;
/**
 * Create a deterministic template loader backed by on-disk stub files.
 *
 * This utility is pack-agnostic and can be reused by any pack implementation
 * to implement file-backed templates.
 */
export declare function createTemplateStubLoader(options: TemplateStubLoaderOptions): TemplateStubLoader;
//# sourceMappingURL=template-stubs.d.ts.map