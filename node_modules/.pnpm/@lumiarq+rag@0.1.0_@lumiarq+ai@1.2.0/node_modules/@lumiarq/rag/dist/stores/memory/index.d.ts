import type { Chunk, IndexedDocument, SearchOpts, SearchResult, VectorStore } from '../../types/index.js';
/**
 * In-memory VectorStore backed by brute-force cosine similarity.
 *
 * Performance: O(n) per query. Suitable for up to ~50k chunks in development
 * and CI. Use a production adapter (pgvector, Qdrant) for larger corpora.
 */
export declare class MemoryVectorStore implements VectorStore {
    private readonly entries;
    /** documentId → latest upsert timestamp */
    private readonly docTimestamps;
    /** documentId → content hash */
    private readonly docContentHashes;
    upsert(docs: IndexedDocument[]): Promise<void>;
    delete(ids: string[]): Promise<void>;
    search(vector: number[], opts?: SearchOpts): Promise<SearchResult[]>;
    getIndexedIds(): Promise<string[]>;
    lastIndexedAt(documentId: string): Promise<Date | null>;
    getByIds(ids: string[]): Promise<Chunk[]>;
    getContentHash(documentId: string): Promise<string | null>;
    /** Number of indexed chunks (useful for tests). */
    get size(): number;
}
//# sourceMappingURL=index.d.ts.map