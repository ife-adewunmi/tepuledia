import type { ISessionRepository, CreateSessionData, Session } from '../contracts/session-repository.js';
/**
 * Creates a new session record with a hashed token.
 * The caller is responsible for hashing the token before passing it.
 */
export declare const CreateSessionTask: (data: CreateSessionData, repo: ISessionRepository) => Promise<Session>;
//# sourceMappingURL=create-session.task.d.ts.map