export function defineLoader(definition) {
    return {
        id: definition.id ?? 'loader',
        dependsOn: definition.dependsOn ?? [],
        fetch: async ({ input, deps }) => {
            const parsed = definition.schema.parse(input);
            return definition.fetch({ input: parsed, deps });
        },
    };
}
export function defineSitemapLoader(fn) {
    return fn;
}
//# sourceMappingURL=define-loader.js.map