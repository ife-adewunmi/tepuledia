# Laravel migration post-step

Lumis intentionally does not generate migration files.

Run the following commands manually:

```sh
php artisan make:migration {{MigrationName}}
php artisan migrate
```

Then update the generated migration file under:

- database/migrations/*.php

Resource hint: {{ResourceName}}
