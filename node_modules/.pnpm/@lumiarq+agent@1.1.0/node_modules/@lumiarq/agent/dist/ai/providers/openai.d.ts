import type { AIProvider, AITask } from '../gateway.js';
export type OpenAIProviderOptions = {
    apiKey: string;
    /** Override the provider ID registered in the gateway. Defaults to `"openai"`.
     *  Set to `"groq"` (or any other string) when pointing `baseUrl` at a different service
     *  so it doesn't collide with a real OpenAI provider in the same gateway. */
    id?: string;
    baseUrl?: string;
    defaultModel?: string;
    taskModels?: Partial<Record<AITask, string>>;
};
export declare function createOpenAIProvider(options: OpenAIProviderOptions): AIProvider;
//# sourceMappingURL=openai.d.ts.map