import type { CoreExecutionPlan, CoreLumisIR, LumisAdapter } from '@lumiarq/context';
import { type ConfirmationHandler } from './confirmation.js';
import { type ExecutionSummary } from './summary.js';
import { type AgentEventListener } from './stream.js';
type AgentTraceStatus = 'start' | 'success' | 'failure' | 'info';
type AgentTraceEvent = {
    type: 'config-load' | 'command-dispatch' | 'plan-execution' | 'runtime-task';
    status: AgentTraceStatus;
    name: string;
    message?: string;
    context?: Record<string, unknown>;
};
type AgentTraceListener = (event: AgentTraceEvent) => void | Promise<void>;
export type ExecutePlanOptions = {
    onEvent?: AgentEventListener;
    onTrace?: AgentTraceListener;
    confirm?: ConfirmationHandler;
    updateIR: (ir: CoreLumisIR, changedFile: string, adapter: LumisAdapter) => Promise<CoreLumisIR>;
    /** Enable strict verifier enforcement — critical hook failures abort execution before any file write. */
    strict?: boolean;
};
export type ExecutePlanResult = {
    ir: CoreLumisIR;
    summary: ExecutionSummary;
};
export declare function executePlan(plan: CoreExecutionPlan, ir: CoreLumisIR, adapter: LumisAdapter, options: ExecutePlanOptions): Promise<ExecutePlanResult>;
export {};
//# sourceMappingURL=agent.d.ts.map