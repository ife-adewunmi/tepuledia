export async function ensureStepConfirmation(step, confirm) {
    if (!step.requiresConfirmation) {
        return;
    }
    const accepted = await confirm(step);
    if (!accepted) {
        throw new Error(`Execution cancelled for step "${step.id}".`);
    }
}
//# sourceMappingURL=confirmation.js.map