import type { StorageContract, StoredFile, PutOptions } from '@illumiarq/contracts';
import type { LoggerContract } from '@illumiarq/contracts';
export interface StubStorageOptions {
    root: string;
    logger: LoggerContract;
}
export declare class StubStorage implements StorageContract {
    private readonly root;
    private readonly logger;
    constructor(opts: StubStorageOptions);
    put(path: string, file: Buffer | ReadableStream, options?: PutOptions): Promise<StoredFile>;
    get(path: string): Promise<Buffer | null>;
    delete(path: string): Promise<void>;
    exists(path: string): Promise<boolean>;
    url(path: string): string;
    temporaryUrl(path: string, _expiry: Date): Promise<string>;
}
//# sourceMappingURL=stub-storage.d.ts.map