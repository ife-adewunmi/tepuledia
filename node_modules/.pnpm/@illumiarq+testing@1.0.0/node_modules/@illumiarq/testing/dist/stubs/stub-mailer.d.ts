import type { MailerContract, MailMessage } from '@illumiarq/contracts';
import type { LoggerContract } from '@illumiarq/contracts';
export interface StubMailerOptions {
    logger: LoggerContract;
}
export declare class StubMailer implements MailerContract {
    private readonly logger;
    readonly sent: MailMessage[];
    constructor(opts: StubMailerOptions);
    send(message: MailMessage): Promise<void>;
    queue(message: MailMessage): Promise<void>;
}
//# sourceMappingURL=stub-mailer.d.ts.map