/**
 * Read source lines around a given line number for context display.
 *
 * @param filePath  Absolute path to the source file
 * @param line      1-based line number to focus on
 * @param radius    Number of lines before/after to include (default: 6)
 * @returns Array of `{ lineNo, code, isFocus }` objects, or empty array if unreadable
 */
export interface ContextLine {
    lineNo: number;
    code: string;
    isFocus: boolean;
}
export declare function readContext(filePath: string, line: number, radius?: number): ContextLine[];
//# sourceMappingURL=reader.d.ts.map