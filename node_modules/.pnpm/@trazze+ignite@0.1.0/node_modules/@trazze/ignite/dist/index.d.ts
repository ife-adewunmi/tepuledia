export { pushToTrazze } from './push.js';
export type { TrazzeIgnitePayload } from './push.js';
export { resolveEditorScheme, buildEditorUrl, editorLabel } from './editors.js';
export type { EditorScheme } from './editors.js';
/**
 * Handle an unhandled error from the framework boot() onError hook.
 *
 * In development: returns a rich HTML error page (Ignition-style) and emits
 *   a structured Trazze-compatible log entry to the console.
 * In production:  returns a plain-text 500 response and still emits the log
 *   so the Trazze dashboard captures the event.
 *
 * Wire this into the LumiARQ boot hooks:
 * ```ts
 * import { handleIgnitionError } from '@trazze/ignite'
 *
 * export default boot({ onError: handleIgnitionError })
 * ```
 */
export declare function handleIgnitionError(err: Error, req: Request): Promise<Response>;
//# sourceMappingURL=index.d.ts.map