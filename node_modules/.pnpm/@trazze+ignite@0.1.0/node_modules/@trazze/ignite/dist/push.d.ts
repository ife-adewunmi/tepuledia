import type { StackFrame } from './types.js';
export interface TrazzeIgnitePayload {
    /** Error name (e.g. "TypeError") */
    name: string;
    /** Error message */
    message: string;
    /** Raw stack string */
    stack: string | undefined;
    /** Parsed frames */
    frames: StackFrame[];
    /** Inbound request method */
    method: string;
    /** Full request URL */
    url: string;
    /** ISO timestamp of when the error was caught */
    timestamp: string;
}
/**
 * Push an ignition error event to the Trazze dashboard ingestion API.
 *
 * Reads the following env vars:
 *   TRAZZE_URL          – base URL of the Trazze server (e.g. http://localhost:3000)
 *   TRAZZE_API_KEY      – API key used for the `x-api-key` header
 *   TRAZZE_PROJECT      – project slug (defaults to "default")
 *   APP_ENV / NODE_ENV  – used as the `environment` field
 *
 * If TRAZZE_URL or TRAZZE_API_KEY is not set this is a no-op (no throw).
 * Failures are swallowed to never interfere with the error response.
 */
export declare function pushToTrazze(err: Error, req: Request): Promise<void>;
//# sourceMappingURL=push.d.ts.map