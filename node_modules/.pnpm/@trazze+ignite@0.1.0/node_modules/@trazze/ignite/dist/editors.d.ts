/**
 * Editor deep-link URL schemes.
 *
 * Reads TRAZZE_EDITOR (or EDITOR) from the environment and returns a URL
 * that opens the given file at the given line in the developer's editor.
 *
 * Supported values for TRAZZE_EDITOR:
 *   vscode          → vscode://file/{path}:{line}:{col}
 *   cursor          → cursor://file/{path}:{line}:{col}
 *   phpstorm        → phpstorm://open?file={path}&line={line}
 *   webstorm        → webstorm://open?file={path}&line={line}
 *   sublime         → subl://{path}:{line}
 *   textmate        → txmt://open?url=file://{path}&line={line}
 *   atom            → atom://core/open/file?filename={path}&line={line}
 *   (unset/unknown) → null  (links are hidden)
 */
export type EditorScheme = 'vscode' | 'cursor' | 'phpstorm' | 'webstorm' | 'sublime' | 'textmate' | 'atom';
export declare function resolveEditorScheme(): EditorScheme | null;
export declare function buildEditorUrl(scheme: EditorScheme, file: string, line: number, column?: number): string;
export declare function editorLabel(scheme: EditorScheme): string;
//# sourceMappingURL=editors.d.ts.map