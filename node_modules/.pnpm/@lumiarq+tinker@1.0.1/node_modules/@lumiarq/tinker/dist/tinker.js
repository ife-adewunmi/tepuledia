import { createTinkerContext } from './context.js';
import { evaluateInContext } from './eval.js';
export async function startTinker(provider) {
    const context = await createTinkerContext(provider);
    return {
        context,
        evaluate: async (code) => evaluateInContext(code, context),
    };
}
//# sourceMappingURL=tinker.js.map