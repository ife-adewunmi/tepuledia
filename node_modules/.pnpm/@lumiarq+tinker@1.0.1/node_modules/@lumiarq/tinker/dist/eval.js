export async function evaluateInContext(code, context) {
    const names = Object.keys(context);
    const values = Object.values(context);
    const AsyncFn = Object.getPrototypeOf(async function noop() { }).constructor;
    const fn = new AsyncFn(...names, `return (async () => (${code}))()`);
    return fn(...values);
}
//# sourceMappingURL=eval.js.map