const ARTIFACT_KIND_PATTERN = /^[a-z][a-z0-9.-]*$/;
const ARTIFACT_NAME_PATTERN = /^[A-Za-z][A-Za-z0-9]*$/;
/**
 * Validate naming conventions for step artifact kind and name.
 *
 * @param ctx - Verification context with active step values.
 * @returns Naming validation result.
 */
export async function checkNaming(ctx) {
    const { artifactKind, name } = ctx.step;
    if (!ARTIFACT_KIND_PATTERN.test(artifactKind)) {
        return {
            passed: false,
            guarantee: 'naming',
            message: `artifactKind "${artifactKind}" must be lowercase kebab/dot style.`,
            severity: 'warning',
        };
    }
    if (!ARTIFACT_NAME_PATTERN.test(name)) {
        return {
            passed: false,
            guarantee: 'naming',
            message: `name "${name}" must be a simple alphanumeric identifier.`,
            severity: 'warning',
        };
    }
    return {
        passed: true,
        guarantee: 'naming',
        message: 'Artifact naming is valid.',
    };
}
//# sourceMappingURL=naming.js.map