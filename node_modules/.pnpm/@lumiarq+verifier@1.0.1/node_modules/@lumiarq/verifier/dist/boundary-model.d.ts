import type { CoreLumisIR } from '@lumiarq/context';
export type BoundaryModel = {
    violations: CoreLumisIR['violations'];
    violationCount: number;
    violationsByRule: Record<string, number>;
};
/**
 * Build a compact boundary-violation model from the current IR.
 *
 * @param ir - Current project IR snapshot.
 * @returns Aggregate boundary model with total count and counts by violated rule.
 */
export declare function createBoundaryModel(ir: CoreLumisIR): BoundaryModel;
//# sourceMappingURL=boundary-model.d.ts.map