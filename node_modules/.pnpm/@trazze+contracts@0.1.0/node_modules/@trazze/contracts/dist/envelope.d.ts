export interface TrazeEnvelope<T = unknown> {
    id: string;
    type: 'log' | 'trace' | 'error' | 'metric';
    timestamp: string;
    project: string;
    environment: string;
    sdk: {
        name: string;
        version: string;
    };
    data: T;
}
//# sourceMappingURL=envelope.d.ts.map