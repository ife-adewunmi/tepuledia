import type { CoreExecutionStep, LumisAdapter } from '@lumiarq/context';
import { type TemplateSource } from './template-resolver.js';
export type GenerateOptions = {
    force?: boolean;
    /** Project-local template override directories. Checked before framework path and pack. */
    templatePaths?: string[];
    /** Optional framework-published stubs directory. Checked before pack built-ins. */
    frameworkStubPath?: string;
};
export type GenerateResult = {
    /** Which layer of the precedence chain provided the template. */
    templateSource: TemplateSource;
};
export declare function generateWithAdapter(step: CoreExecutionStep, adapter: LumisAdapter, options?: GenerateOptions): Promise<GenerateResult>;
//# sourceMappingURL=generator-core.d.ts.map