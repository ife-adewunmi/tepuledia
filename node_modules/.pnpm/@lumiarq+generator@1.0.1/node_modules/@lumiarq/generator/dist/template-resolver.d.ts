import type { LumisAdapter } from '@lumiarq/context';
/**
 * Where a resolved template came from. Emitted as provenance in every resolution result.
 * - `project`: a file from the developer's own template override paths.
 * - `framework`: a file from the optional framework-published stubs path.
 * - `pack`: the adapter's built-in template (fallback).
 */
export type TemplateSource = 'project' | 'framework' | 'pack';
export type ResolvedTemplate = {
    content: string;
    source: TemplateSource;
    resolvedPath?: string;
};
export type TemplateResolverOptions = {
    /**
     * Zero or more project-local directories to check first.
     * Each directory is searched for `<templateName>` (exact) then `<templateName>.stub`.
     */
    projectTemplatePaths?: string[];
    /**
     * Optional path to framework-published stubs. Searched after project paths.
     * Same lookup order: exact match, then `.stub` extension.
     */
    frameworkStubPath?: string;
};
/**
 * Resolve a template using the three-layer precedence chain:
 * 1. project override paths (highest priority)
 * 2. framework published stubs path (optional)
 * 3. pack built-in via adapter.resolveTemplate (fallback, always present)
 *
 * Identical inputs always return the same `source` and `content`, satisfying the
 * determinism requirement. The `resolvedPath` field is set for file-backed sources
 * and undefined for pack built-ins.
 */
export declare function resolveTemplate(templateName: string, adapter: LumisAdapter, options?: TemplateResolverOptions): ResolvedTemplate;
//# sourceMappingURL=template-resolver.d.ts.map