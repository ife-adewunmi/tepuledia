export function fillTokens(template, tokens) {
    return Object.entries(tokens).reduce((out, [key, val]) => {
        return out.replaceAll(`{{${key}}}`, val);
    }, template);
}
//# sourceMappingURL=token-fill.js.map