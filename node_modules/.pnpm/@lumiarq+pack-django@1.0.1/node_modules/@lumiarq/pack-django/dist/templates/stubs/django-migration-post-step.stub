# Django migration post-step

Lumis intentionally does not generate migration files.

Run the following commands manually:

```sh
python manage.py makemigrations {{AppName}} --name {{MigrationName}}
python manage.py migrate
```

Then validate generated files under:

- */migrations/*.py

Model hint: {{ModelName}}
