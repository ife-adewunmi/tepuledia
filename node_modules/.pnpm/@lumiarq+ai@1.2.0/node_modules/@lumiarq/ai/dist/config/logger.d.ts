/**
 * Minimal logger interface that @lumiarq/ai accepts.
 * Any logger satisfying this shape works — including @trazze/sdk Logger,
 * console, or any custom implementation. No hard dep on @trazze/contracts.
 */
export interface LumiarqLogger {
    debug(msg: string, ctx?: Record<string, unknown>): void | Promise<void>;
    info(msg: string, ctx?: Record<string, unknown>): void | Promise<void>;
    warn(msg: string, ctx?: Record<string, unknown>): void | Promise<void>;
    error(msg: string, ctx?: Record<string, unknown>): void | Promise<void>;
    /**
     * Optional performance measurement helper.
     * If omitted, the gateway falls back to calling the wrapped function directly.
     */
    measure?<T>(label: string, fn: () => Promise<T>, ctx?: Record<string, unknown>): Promise<T>;
}
//# sourceMappingURL=logger.d.ts.map