/**
 * Mistral AI provider.
 *
 * https://mistral.ai
 * Env: MISTRAL_API_KEY
 *
 * Mistral uses an OpenAI-compatible chat completions API.
 * `mistral-small-latest` is the fastest general model;
 * `mistral-large-latest` (or `mistral-medium-latest`) for harder tasks.
 */
import type { AIProvider, AITask } from '../gateway/gateway.ts';
export type MistralProviderOptions = {
    apiKey: string;
    defaultModel?: string;
    taskModels?: Partial<Record<AITask, string>>;
};
/**
 * Creates a Mistral provider.
 *
 * @param options Options for configuring the Mistral provider.
 * @returns An AIProvider instance configured for Mistral.
 */
export declare function createMistralProvider(options: MistralProviderOptions): AIProvider;
//# sourceMappingURL=mistral.d.ts.map