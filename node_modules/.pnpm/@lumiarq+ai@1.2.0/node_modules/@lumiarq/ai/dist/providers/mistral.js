import { createOpenAIProvider } from './openai.js';
import { PROVIDER_DEFAULT_MODELS } from '../config/models.js';
import { AI_PROVIDER_URL_PATH } from '../config/paths.js';
/**
 * Creates a Mistral provider.
 *
 * @param options Options for configuring the Mistral provider.
 * @returns An AIProvider instance configured for Mistral.
 */
export function createMistralProvider(options) {
    return createOpenAIProvider({
        id: 'mistral',
        apiKey: options.apiKey,
        baseUrl: AI_PROVIDER_URL_PATH.mistral,
        defaultModel: options.defaultModel ?? (PROVIDER_DEFAULT_MODELS['mistral']?.['token-suggest'] ?? 'mistral-small-latest'),
        taskModels: { ...PROVIDER_DEFAULT_MODELS['mistral'], ...options.taskModels },
    });
}
//# sourceMappingURL=mistral.js.map