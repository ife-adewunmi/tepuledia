/**
 * OpenRouter provider — unified gateway to 200+ AI models.
 *
 * https://openrouter.ai
 * Env: OPENROUTER_API_KEY
 *
 * Supports the same OpenAI-compatible chat completions API.
 * Pass `appUrl` and `appTitle` for attribution on the OpenRouter dashboard.
 */
import type { AIProvider, AITask } from '../gateway/gateway.ts';
export type OpenRouterProviderOptions = {
    apiKey: string;
    /** Your app's public URL for attribution in the OpenRouter dashboard. */
    appUrl?: string;
    /** Your app's display name shown in OpenRouter dashboard. */
    appTitle?: string;
    defaultModel?: string;
    taskModels?: Partial<Record<AITask, string>>;
};
/**
 * Create an OpenRouter provider instance.
 *
 * @param options Configuration options for the OpenRouter provider.
 * @returns An AIProvider instance configured for OpenRouter.
 */
export declare function createOpenRouterProvider(options: OpenRouterProviderOptions): AIProvider;
//# sourceMappingURL=openrouter.d.ts.map