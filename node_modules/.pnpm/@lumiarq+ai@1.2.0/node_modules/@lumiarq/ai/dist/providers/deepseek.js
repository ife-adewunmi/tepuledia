import { createOpenAIProvider } from './openai.js';
import { PROVIDER_DEFAULT_MODELS } from '../config/models.js';
import { AI_PROVIDER_URL_PATH } from '../config/paths.js';
/**
 * Creates a DeepSeek provider.
 *
 * @param options Options for configuring the DeepSeek provider.
 * @returns An AIProvider instance configured for DeepSeek.
 */
export function createDeepSeekProvider(options) {
    return createOpenAIProvider({
        id: 'deepseek',
        apiKey: options.apiKey,
        baseUrl: AI_PROVIDER_URL_PATH.deepseek,
        defaultModel: options.defaultModel ?? (PROVIDER_DEFAULT_MODELS['deepseek']?.['token-suggest'] ?? 'deepseek-chat'),
        taskModels: { ...PROVIDER_DEFAULT_MODELS['deepseek'], ...options.taskModels },
    });
}
//# sourceMappingURL=deepseek.js.map