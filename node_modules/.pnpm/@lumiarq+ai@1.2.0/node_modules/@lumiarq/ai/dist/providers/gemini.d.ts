import type { AIProvider, AITask } from '../gateway/gateway.ts';
export type GeminiProviderOptions = {
    apiKey: string;
    baseUrl?: string;
    defaultModel?: string;
    taskModels?: Partial<Record<AITask, string>>;
};
/**
 * Gemini provider — Google's advanced language models.
 *
 * https://ai.google.dev/gemini
 * Env: GEMINI_API_KEY or GOOGLE_AI_API_KEY
 *
 * @param options Configuration options for the Gemini provider.
 * @returns An AIProvider instance configured for Gemini.
 *
 * Supports both chat completions and embeddings via the same API, with provider-prefixed model names
 * (e.g. `gemini:text-embedding-004`) or auto-resolved models based on task type.
 */
export declare function createGeminiProvider(options: GeminiProviderOptions): AIProvider;
//# sourceMappingURL=gemini.d.ts.map