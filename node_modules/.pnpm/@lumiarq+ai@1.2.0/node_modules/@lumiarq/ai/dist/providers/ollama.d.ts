/**
 * Ollama provider — local LLM runtime.
 *
 * Requires Ollama running locally: https://ollama.com
 * Default base URL: http://localhost:11434/v1  (OpenAI-compatible endpoint)
 *
 * Pull a model first: `ollama pull llama3.2`
 * Then use OLLAMA_BASE_URL (optional) to point at a non-default host.
 */
import type { AIProvider, AITask } from '../gateway/gateway.ts';
export type OllamaProviderOptions = {
    /** Base URL of the Ollama OpenAI-compatible API. Defaults to `http://localhost:11434/v1`. */
    baseUrl?: string;
    /** Default model to use when none is specified. Defaults to `llama3.2`. */
    defaultModel?: string;
    /** Per-task model overrides. */
    taskModels?: Partial<Record<AITask, string>>;
};
/**
 * Creates an Ollama provider.
 *
 * @param options Options for configuring the Ollama provider.
 * @returns An AIProvider instance configured for Ollama.
 */
export declare function createOllamaProvider(options?: OllamaProviderOptions): AIProvider;
//# sourceMappingURL=ollama.d.ts.map