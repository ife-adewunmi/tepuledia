/**
 * DeepSeek provider — cost-effective reasoning models.
 *
 * https://deepseek.com
 * Env: DEEPSEEK_API_KEY
 *
 * DeepSeek uses an OpenAI-compatible API.
 * `deepseek-chat` is the fastest; `deepseek-reasoner` (R1) is better for planning.
 */
import type { AIProvider, AITask } from '../gateway/gateway.ts';
export type DeepSeekProviderOptions = {
    apiKey: string;
    defaultModel?: string;
    taskModels?: Partial<Record<AITask, string>>;
};
/**
 * Creates a DeepSeek provider.
 *
 * @param options Options for configuring the DeepSeek provider.
 * @returns An AIProvider instance configured for DeepSeek.
 */
export declare function createDeepSeekProvider(options: DeepSeekProviderOptions): AIProvider;
//# sourceMappingURL=deepseek.d.ts.map