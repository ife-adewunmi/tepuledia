import { createOpenAIProvider } from './openai.js';
import { PROVIDER_DEFAULT_MODELS } from '../config/models.js';
import { AI_PROVIDER_URL_PATH } from '../config/paths.js';
/**
 * Creates an Ollama provider.
 *
 * @param options Options for configuring the Ollama provider.
 * @returns An AIProvider instance configured for Ollama.
 */
export function createOllamaProvider(options = {}) {
    return createOpenAIProvider({
        id: 'ollama',
        // Ollama's OpenAI-compat endpoint doesn't require a real API key.
        apiKey: 'ollama',
        baseUrl: options.baseUrl ?? AI_PROVIDER_URL_PATH.ollama,
        defaultModel: options.defaultModel ?? (PROVIDER_DEFAULT_MODELS['ollama']?.['token-suggest'] ?? 'llama3.2'),
        taskModels: { ...PROVIDER_DEFAULT_MODELS['ollama'], ...options.taskModels },
    });
}
//# sourceMappingURL=ollama.js.map