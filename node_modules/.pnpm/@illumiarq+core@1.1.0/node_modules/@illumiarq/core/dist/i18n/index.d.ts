export declare function setLocaleConfig(locale: string, fallbackLocale: string): void;
export declare function loadTranslations(locale: string, translations: Record<string, string>): void;
export declare function getActiveLocale(): string;
/**
 * Core translation function.
 *
 * Looks up `key` in the active locale first, then the fallback locale.
 * Returns the raw `key` string if not found in either — never throws.
 *
 * Parameter interpolation: replaces `:key` tokens with `params[key]`.
 */
export declare function t(key: string, params?: Record<string, string | number>): string;
//# sourceMappingURL=index.d.ts.map